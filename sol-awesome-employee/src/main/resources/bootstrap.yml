spring:
  profiles:
    active: local
  application:
    name: sol-awesome-employee    
  
  cloud:
    services:
      registrationMethod: route

logging:
  level:
    com:
      sun:
        proxy: INFO
      sol:
        awesome:
          filter:
            SimpleCORSFilter: INFO
